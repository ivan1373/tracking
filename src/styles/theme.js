import { createTheme } from "@mui/material/styles";

const theme = {
	palette: {
		primary: {
			main: "#21717b",
		},
		secondary: {
			main: "#dbb33f",
		},
	},
	shape: {
		borderRadius: 8,
	},
};

export default createTheme(theme);
